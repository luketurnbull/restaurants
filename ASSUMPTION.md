# Assumptions

- **Hardcoded distances** — could use geolocation API + Google location search to calculate real distances
- **Service types (Takeaway, Order Online) hardcoded** — only `dineIn` exists in API data
- **Single image only** — no carousel; API returns one `imageLink` per restaurant
- **"New" badge not shown** — no data field in API to determine new restaurants
